# 🔧 Firebase 설정 문제 해결 가이드

## "작업에 실패했습니다(사유: [object ProgressEvent])" 에러

이 에러는 Firebase 콘솔에서 작업 중 발생하는 일반적인 에러입니다.

### 해결 방법

#### 1️⃣ 브라우저 새로고침 및 재시도
```
1. Ctrl + F5 (강력 새로고침)
2. 이전 작업 다시 시도
3. 여러 번 시도 필요할 수 있음
```

#### 2️⃣ 브라우저 캐시 삭제
```
Chrome:
1. Ctrl + Shift + Delete
2. "캐시된 이미지 및 파일" 선택
3. "데이터 삭제"
4. 브라우저 재시작
```

#### 3️⃣ 시크릿 모드(Incognito) 사용
```
1. Ctrl + Shift + N (Chrome)
2. Firebase 콘솔 재접속
3. 작업 재시도
```

#### 4️⃣ 다른 브라우저 사용
```
- Chrome → Edge 또는 Firefox로 전환
- 확장 프로그램 간섭 방지
```

#### 5️⃣ 네트워크 연결 확인
```
1. 인터넷 연결 상태 확인
2. VPN 사용 중이면 비활성화
3. 방화벽/보안 프로그램 확인
```

---

## 단계별 Firebase 설정 (수동)

### A. Firebase 프로젝트 생성이 안 되는 경우

**대안: Firebase CLI로 프로젝트 확인**

이미 생성된 프로젝트가 있는지 확인:
```bash
# Firebase 로그인 (브라우저 열림)
firebase login

# 프로젝트 목록 확인
firebase projects:list
```

**새 프로젝트 생성:**
1. https://console.firebase.google.com/
2. 로그인
3. "프로젝트 추가" 버튼 여러 번 클릭 시도
4. 페이지 새로고침 후 재시도

---

### B. Authentication 활성화 실패 시

**방법 1: 한 번에 하나씩 활성화**
```
1. 이메일/비밀번호만 먼저 활성화
2. 저장
3. Google 로그인 별도로 활성화
4. 저장
```

**방법 2: REST API로 활성화** (고급)
```bash
# Firebase CLI 사용
firebase auth:import users.json --project cooktalk-mvc
```

---

### C. Firestore 생성 실패 시

**증상**: "데이터베이스 만들기" 버튼 클릭 시 무한 로딩

**해결 방법:**
```
1. 페이지 새로고침 (F5)
2. 다시 "데이터베이스 만들기" 클릭
3. 위치 선택: asia-northeast3 (Seoul)
4. 프로덕션 모드 선택
5. 완료 대기 (최대 5분)
```

**대안: 다른 리전 선택**
```
- asia-northeast1 (Tokyo)
- us-central1 (Iowa)
```

---

### D. Storage 생성 실패 시

**해결 방법:**
```
1. Firestore 먼저 생성 완료
2. Storage 생성 시도
3. 같은 리전 선택 (asia-northeast3)
```

---

### E. 보안 규칙 배포 실패 시

**증상**: "게시" 클릭 시 에러 또는 무반응

**해결 방법 1: 규칙 검증**
```
1. 규칙 탭에서 "검증" 버튼 클릭
2. 오류 확인
3. 문법 오류 수정
4. 다시 "게시" 클릭
```

**해결 방법 2: Firebase CLI로 배포**
```bash
# Firestore 규칙 배포
firebase deploy --only firestore:rules --project cooktalk-mvc

# Storage 규칙 배포
firebase deploy --only storage --project cooktalk-mvc
```

---

### F. Android 앱 등록 실패 시

**해결 방법:**
```
1. 패키지명 정확히 입력: com.example.cooktalk_mvc
2. SHA-1은 나중에 추가 가능 (지금 건너뛰기)
3. google-services.json 다운로드만 완료
```

**google-services.json 다운로드 실패 시:**
```
1. 프로젝트 설정 → 일반 탭
2. "앱" 섹션에서 Android 앱 찾기
3. google-services.json 재다운로드
```

---

## 🛠️ FlutterFire Configure 에러

### "No Firebase project found"

**원인**: Firebase CLI가 프로젝트를 찾지 못함

**해결:**
```bash
# Firebase 로그인 확인
firebase login --reauth

# 프로젝트 선택
firebase use cooktalk-mvc

# FlutterFire 재실행
flutterfire configure --project=cooktalk-mvc
```

### "Permission denied"

**원인**: Firebase 프로젝트 권한 부족

**해결:**
```
1. Firebase 콘솔 → 프로젝트 설정
2. "사용자 및 권한" 확인
3. 본인 계정이 "소유자" 또는 "편집자"인지 확인
```

---

## 🚀 최소 설정으로 시작하기

위 방법들이 모두 실패하면, **최소 설정**으로 시작:

### 1. Firebase 콘솔에서 수동 설정

**최소 요구사항:**
- ✅ Firebase 프로젝트 생성
- ✅ Authentication 활성화 (이메일/비밀번호만)
- ✅ Firestore 생성 (테스트 모드)
- ✅ Android 앱 등록 + google-services.json

**나중에 추가:**
- Storage (이미지 없이 텍스트만 먼저)
- Google 로그인 (이메일 로그인 먼저)
- 보안 규칙 (테스트 모드로 먼저 진행)

### 2. 테스트 모드 규칙 (임시)

**Firestore - 테스트 모드:**
```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
```

**Storage - 테스트 모드:**
```javascript
rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
```

⚠️ **주의**: 테스트 모드는 임시입니다. 나중에 프로덕션 규칙으로 교체 필수!

---

## 📞 추가 지원

### Firebase 상태 확인
https://status.firebase.google.com/

### Firebase 공식 문서
https://firebase.google.com/docs/flutter/setup

### Flutter 커뮤니티
https://stackoverflow.com/questions/tagged/flutter+firebase

---

## ✅ 설정 완료 확인 체크리스트

설정이 완료되었는지 확인:

```bash
# 1. google-services.json 존재 확인
dir android\app\google-services.json

# 2. Firebase 초기화 확인
flutter run

# 로그에서 확인:
# [INFO] Firebase initialized successfully
# [INFO] Firestore offline persistence enabled
```

### 앱에서 테스트:
1. ✅ 회원가입 (이메일/비밀번호)
2. ✅ 로그인
3. ✅ Firebase 콘솔 → Authentication에서 사용자 확인

---

**마지막 수단**: 프로젝트 삭제 후 재생성
```
1. Firebase 콘솔 → 프로젝트 설정
2. "프로젝트 삭제"
3. 처음부터 다시 생성
```

문제가 계속되면 구체적인 에러 메시지를 알려주세요!
