# Phase 3: 인기(트렌딩) 탭 목업 데이터 적용

## 개요
인기 탭(TrendingView)에서 실제 API 대신 목업 데이터를 사용하도록 변경합니다.

---

## 현재 상태 분석

### 현재 파일: `lib/views/trending_view.dart`
```dart
final rc = context.watch<RecipeController>();
// rc.trending - 트렌딩 레시피 목록
// rc.loadTrending() - 데이터 로드
```

### 데이터 흐름 변경
```
TrendingView
  → RecipeController.loadTrending()
  → RecipeRepository.getTrendingRecipes()
  → RecipeService.fetchTrending()
  → MockData.trendingRecipes (목업)
```

---

## 구현 작업

### 1. RecipeService 수정 (`lib/data/services/recipe_service.dart`)

```dart
Future<List<Recipe>> fetchTrending() async {
  if (_useMockData) {
    await Future.delayed(const Duration(milliseconds: 300));
    return _convertMockToRecipes(MockData.trendingRecipes);
  }

  // 실제 API 호출 (MVP 이후)
  return [];
}
```

### 2. TrendingCard 위젯 확인 (`lib/widgets/trending_card.dart`)

TrendingCard는 rank(순위)를 표시하므로, viewCount 기준으로 정렬된 목업 데이터가 필요합니다.

```dart
// 현재 사용법
TrendingCard(recipe: rc.trending[i], rank: i + 1)
```

---

## 표시되는 목업 데이터 (viewCount 순)

| 순위 | 레시피명 | 조회수 | 좋아요 | 태그 |
|-----|---------|--------|--------|------|
| 1 | 마라탕 | 15,420 | 892 | 중식, 마라, 매운맛 |
| 2 | 떡볶이 | 12,350 | 756 | 한식, 분식, 매운맛 |
| 3 | 치킨 샐러드 | 9,870 | 543 | 양식, 샐러드, 다이어트 |
| 4 | 참치김밥 | 8,920 | 421 | 한식, 분식, 도시락 |
| 5 | 카레라이스 | 7,650 | 389 | 일식, 카레, 메인 |
| 6 | 닭갈비 | 6,540 | 298 | 한식, 고기, 매운맛 |

---

## TrendingCard UI 구성

```
┌─────────────────────────────────┐
│  [1] 🏆                         │  ← 순위 배지
│  ┌─────────────────────────┐   │
│  │      레시피 이미지       │   │
│  │                         │   │
│  └─────────────────────────┘   │
│  마라탕                         │  ← 제목
│  ⏱️ 35분  ⭐ 4.6               │  ← 시간, 평점
│  👁️ 15,420  ❤️ 892             │  ← 조회수, 좋아요
│  #중식 #마라 #매운맛           │  ← 태그
└─────────────────────────────────┘
```

---

## 체크리스트

- [ ] RecipeService.fetchTrending() 목업 데이터 반환 구현
- [ ] 조회수(viewCount) 기준 정렬 확인
- [ ] TrendingCard에 순위 표시 확인
- [ ] 2x3 그리드 레이아웃 확인

---

## 테스트 시나리오

1. 앱 실행 → 인기 탭 선택
2. 6개의 트렌딩 레시피가 그리드로 표시되는지 확인
3. 순위 배지(1, 2, 3...) 표시 확인
4. 조회수 높은 순으로 정렬되어 있는지 확인
5. Pull-to-refresh 동작 확인

---

## 다음 단계
Phase 4에서 피드 탭 목업 데이터를 적용합니다.
